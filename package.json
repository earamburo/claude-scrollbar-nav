{
  "name": "claude-minimap-navigator",
  "version": "1.0.0",
  "description": "Visual minimap scrollbar for Claude.ai conversations",
  "scripts": {
    "build": "tsc && npm run copy",
    "copy": "cp manifest.json dist/ && cp styles.css dist/ && cp popup.html dist/",
    "watch": "tsc --watch",
    "dev": "npm run build && web-ext run --source-dir dist --target chromium",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.9.3",
    "web-ext": "^10.1.0"
  },
  "keywords": [
    "chrome-extension",
    "claude",
    "navigation",
    "minimap"
  ],
  "author": "",
  "license": "MIT"
}
